# Pinned to versions actually installed and verified in .venv (uv-managed) as of 2026-07-30.
# M1 (dataset builder) + M2 (baseline -> Optuna best) dependencies.
# pandas capped at <3 and pyarrow at <20 -- both required by mlflow>=2.14,<3.0
# (mlflow 2.x does not yet support pandas 3.x; see decisions.md).
psycopg2-binary==2.9.12
sqlalchemy==2.0.51
pandas==2.3.3
numpy==2.4.6
pyarrow==19.0.1
scikit-learn==1.9.0
xgboost==3.2.0
lightgbm==4.7.0
optuna==4.9.0
mlflow==2.22.5
python-dotenv==1.2.2
pyyaml==6.0.3
matplotlib==3.11.1
seaborn==0.13.2
jupyterlab==4.6.2
nbconvert==7.17.1
ipykernel==7.3.0

# M3+ dependencies (calibration/conformal/SHAP/drift/serving) are NOT yet pinned
# here -- `uv add shap` failed to resolve on Python 3.11 in this environment
# (pulled shap==0.51.0 -> numba==0.53.1/llvmlite==0.36.0, which only supports
# Python <3.10). Deferred until M4/M5/M6 so a real compatible version can be
# verified rather than pinning something unproven. Needed later: shap, mapie,
# evidently, fastapi, uvicorn, pydantic.
