# Pinned to versions actually installed and verified in .venv (uv-managed) as of 2026-07-30.
# M1 (dataset builder) + M2 (baseline -> Optuna best) dependencies.
# pandas capped at <3 and pyarrow at <20 -- both required by mlflow>=2.14,<3.0
# (mlflow 2.x does not yet support pandas 3.x; see decisions.md).
psycopg2-binary==2.9.12
sqlalchemy==2.0.51
pandas==2.3.3
numpy==2.4.6
pyarrow==19.0.1
scikit-learn==1.9.0
xgboost==3.2.0
lightgbm==4.7.0
optuna==4.9.0
mlflow==2.22.5
python-dotenv==1.2.2
pyyaml==6.0.3
matplotlib==3.11.1
seaborn==0.13.2
jupyterlab==4.6.2
nbconvert==7.17.1
ipykernel==7.3.0

# M4: shap. Plain `uv add shap` picks shap==0.51.0 but resolves an old
# numba==0.53.1/llvmlite==0.36.0 pair that only supports Python <3.10 --
# fails to build on this project's Python 3.11. Constraining the range
# (shap>=0.48,<0.52) makes uv's resolver land on the same shap==0.51.0 but
# with numba==0.66.0/llvmlite==0.48.0 instead, which builds and imports
# cleanly. See decisions.md M4 entry.
shap==0.51.0
numba==0.66.0
llvmlite==0.48.0

# M5: conformal prediction + serving. mapie==1.4.1 is a major-version jump from
# the classic `MapieClassifier(method="score")` API documented in most MAPIE
# tutorials -- v1.x renamed it to `SplitConformalClassifier(conformity_score="lac")`
# (see core/conformal.py's docstring and decisions.md's M5 entry).
mapie==1.4.1
fastapi==0.141.1
uvicorn==0.52.0
pydantic==2.13.4
requests==2.34.2
httpx==0.28.1

# M6: drift monitoring. evidently==0.7.21 is the new Report/Dataset/Preset
# API (Report(metrics=[DataDriftPreset()]).run(...)), a full rewrite from the
# classic 0.4.x ColumnMapping/Dashboard API most tutorials show -- see
# core/monitoring/drift_base.py. NOTE: evidently pulls in nltk transitively;
# nltk 2026's CWD-import security hook false-positives in this project
# because .venv/ lives inside the repo root -- `make drift` and CI both set
# NLTK_DISABLE_IMPORT_SECURITY=1 (NLTK's own documented escape hatch for this
# exact case, see decisions.md M6 entry).
evidently==0.7.21
